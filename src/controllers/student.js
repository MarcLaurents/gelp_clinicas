// Manipulação dos dados, pré alteração no banco.
const studentController = {
    getAll: async(req, res) => {},
    getOne: async(req, res) => {},
    create: async(req, res) => {},
    update: async(req, res) => {},
    remove: async (req, res) => {}
}

module.exports = studentController;
